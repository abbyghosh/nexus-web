import React from "react";

function NotFound() {
  return (
    <div
      style={{
        display: "grid",
        placeItems: "center",
        height: "80vh",
        fontWeight: 700,
        fontSize: 20,
      }}
    >
      404 | COULD NOT LOCATE PAGE.
    </div>
  );
}

export default NotFound;
